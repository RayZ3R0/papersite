# Public URL for the API proxy (used by the frontend)
NEXT_PUBLIC_API_URL="http://localhost:3000/api/papers"

# Private URL for the actual PaperNexus API (used by the backend proxy)
PAPERVOID_API_URL="<your-api-url-here>"
